services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: capacity
      POSTGRES_PASSWORD: capacity
      POSTGRES_DB: capacity
    ports:
      - "5432:5432"
    volumes:
      - capacity_postgres:/var/lib/postgresql/data

volumes:
  capacity_postgres:
